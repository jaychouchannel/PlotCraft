# 服务端主加密密钥，用于加密存储 AI api key
# 生成方式：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ONE_ENCRYPT_KEY=替换为你生成的Fernet密钥

# 后端监听
HOST=127.0.0.1
PORT=8000

# 数据库位置
DB_PATH=./data.db
